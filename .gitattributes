# Keep source and generated parity deterministic across platforms.
* text=auto eol=lf

# plugin.js is a committed build artifact (esbuild output of src/) so the repo
# installs directly through `hermes plugins install` with no build step on the
# consumer side. scripts/check-artifact.mjs guards it against drift.
plugin.js linguist-generated
